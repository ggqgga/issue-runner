# scripts — 머리 주석에서 옮겨 온 사고 이력·설계 근거 (한글 전용)

`scripts/*.sh` 의 머리 주석은 **무엇을 · 입력/출력 계약 · 불변식** 만 담는다. 그 계약이
**왜** 그렇게 생겼는가(어떤 사고 `#NNN` 가 세웠는가 · 어떤 대안을 왜 버렸는가 · 어느
attempt 가 무엇을 놓쳤는가 · 언제 무엇을 실측했는가)는 이 문서가 소유한다. 각 스크립트
머리 주석의 마지막 줄 `# 근거: references/scripts-rationale.md <스크립트명> §N` 이 여기
절 번호를 가리킨다.

**스크립트를 호출하는 데에는 읽지 않아도 된다.** 규칙을 바꾸려 할 때·되돌리려 할 때
읽는다. 여기 적힌 어떤 문장도 지시가 아니다 — 지시는 전부 머리 주석과 코드에 있다.
반대로 여기서 사라진 근거는 다음 구현자가 같은 사고를 다시 낸다는 뜻이므로 지우지 말고
옮겨라.

영문판은 없다(`closeout-rationale.md` 와 같은 결정 — 매 틱 로드되지 않는 문서에 번역
동기 비용을 들이지 않는다).

절 번호는 **스크립트마다 `§1` 부터 다시 센다.** 포인터가 `<스크립트명> §N` 형태인 이유다.

문서 색인:
- [loop-status.sh](#loop-statussh) §1–§11
- [finish-classify.sh](#finish-classifysh) §1–§9
- [transition.sh](#transitionsh) §1–§8
- [bounce-state.sh](#bounce-statesh) §1–§6
- [progress-evidence.sh](#progress-evidencesh) §1–§4

---

## loop-status.sh

절 색인: §1 존재 이유 · §2 버킷 이름과 표시 순서 · §3 needs-human 이 가장 앞인 이유 ·
§4 배포대기 제목 폴백 · §5 막힘 버킷과 블로커 파싱 · §6 warn 의 정의 ·
§7 무소속 PR·인계 전 창·워커 사망 의심 · §8 미러 불일치와 정지 미러 불일치 ·
§9 목록 절단 warn 에서 닫힌 이슈를 뺀 이유 · §10 에픽 절과 닫힌 leaf 의 창 ·
§11 gh 호출 예산과 검색 금지의 예외

### §1 존재 이유 (#144)

세 루프(issue-runner·verify-runner·closeout)의 ④ Report 는 "이 틱에 한 일" 카운터뿐이라
파이프라인에 무엇이 쌓여 있는지는 아무도 안 본다. 이 스크립트는 **GitHub 라벨·PR 상태만
읽어**(로컬 상태 파일 없음 · 쓰기 0 — 순수 읽기) 그 재고를 찍는다. 세 루프가 ④ Report
끝에 이 출력을 그대로 붙이고, 사람도 손으로 친다.

버킷은 `references/state-machine.md`(#393) 의 상태 행을 대시보드 칸으로 접은 것이다 —
상태·소유·회수는 그 표, 머리 주석은 판별식.

### §2 버킷 이름과 표시 순서 (#276, 사용자 결정)

버킷 이름은 **"누가 들고 있나"** 다: `X대기` = 그 루프가 집기 전, 루프 이름
(`issue-runner`·`verify-runner`·`closeout`) = 그 루프가 지금 들고 있음. 표시 순서는
사다리 앞→뒤다. jq 키(`--json` 스키마)는 종전 이름 그대로 두고 표시 문구만 바꿨다
(`verifying` 키만 #276 에서 신설).

`보류` 만은 표시 이름을 루프 이름으로 바꾸지 않았다 — 어느 루프도 안 들고 있고 루프가
스스로 재개하는 칸이라 붙일 루프 이름이 없다. 표시 순서는 needs-human 줄 **앞**(사다리
아래 · 사람 정지 위).

`보류` 가 **단계 라벨보다 앞**인 이유: 기계가 멈춘 건은 그 단계를 들고 있는 루프가 없다.
이 칸이 없으면 그 이슈들이 전부 `대기`(= 집을 수 있는 이슈) 로 떨어져, needs-human 칸이
"손댈 게 없는 것" 으로 찼던 오류(#244)의 **반대 방향**이 된다.

단독 `hold:conflict` 가 `보류` 이지 needs-human 이 아닌 이유는 #345 다 — 창
(`RESUME_AFTER_MIN`) 뒤 재개 스윕이 1회 되돌리는 기계 정지라 `hold:ladder` 와 같은 칸이다.
옛 정의 "충돌은 그 자체가 사람 몫" 은 #344/#345 로 사유가 갈리기 전의 것이다.

`eligible-issues.sh` 의 `blocker_state_of` 가 needs-human 과 같은 집합을 읽는다 —
갈라지면 블로커 카운트가 어긋난다.

### §3 needs-human 이 가장 앞인 이유 (2026-09-13, BoDAT #5197)

deploy-cycle 이 승격·배포 실패에 `needs-human` 을 붙이게 됐고(숨은 정지 파일 폐지 — 사람
몫은 이 라벨 하나), 사용자는 이 라벨만 보기로 했으므로 배포대기·테스트 이슈에 붙은 것도
needs-human 칸으로 온다. 옛 순서(배포대기가 이김, #243)는 그 실패 표식을 배포대기 칸에
숨겼다.

`질문 없음` 표시(#157)의 마커를 **지금 붙어 있는 사유**로 가리는 이유는 #160 이다 —
코멘트는 홀드가 풀려도 남으므로, 사유를 안 가리면 옛 `policy` 질문이 지금의 `conflict`
홀드를 가린다. `policy`·`conflict` 는 `--note`(사람이 답해야 할 질문 한 줄)가 필수라
질문이 없으면 사람은 무엇을 답할지 모른다. 질문이 없는 건은 옛 전이가 라벨만 붙이고
코멘트에 실패해 남긴 잔여물이거나(#157 이전), 사람이 손으로 붙인 홀드다. `ladder` 는
`--note` 가 선택이라 대상이 아니다.

맨 `needs-human`(사유 라벨 없음)이 warn 이 아니라 **note** 인 이유도 #244 다 — 기계 정지가
사유 라벨만 붙이게 된 뒤로 맨 `needs-human` 은 "사람이 직접 세웠다" 하나만 뜻하는 **정상
상태**다. warn 은 루프가 교정 가능한 위반일 때만이다(§6).

### §4 배포대기 제목 폴백 (라벨 도입 전)

실측 BoDAT 은 `배포 대기:`·`배포 검증:` 두 형식이 섞여 있고 콜론 앞 공백도 들쭉날쭉이라
`^배포 (대기|검증)` 로 본다 — 콜론을 앵커로 걸지 않는다(`배포 대기 (승격만) — …` 형태가
실존).

제목 폴백에 **사다리 라벨 0개** 게이트가 필요한 이유: 제목만 보면 아직 구현·검증이 도는
이슈(`flow:verify` 등)가 배포대기로 새어 "배포만 기다린다" 로 읽힌다.

### §5 막힘 버킷과 블로커 파싱 (#248)

`막힘` 은 `대기` 의 갈래라 `대기` 바로 아래 줄에 그린다. 다른 버킷(1~8)은 블로커와
무관하게 그대로다 — 루프가 이미 들고 있는 건에 "막혔다" 를 덧씌우면 신호가 겹친다.
`막힘` 은 `대기` 에서 옮겨 온 것이라 `열림 N` 합은 #248 앞뒤로 변하지 않는다.

블로커 파싱 규칙의 SSOT 는 `eligible-issues.sh`(그 파일의 `body_blockers`/`label_blockers`
주석)이고, loop-status 의 jq 는 같은 규칙을 옮겨 적은 것이다. 한쪽을 고치면 다른 쪽도 같이
고쳐라 — 같은 계산을 두 곳에서 다르게 하면, 사람 눈에 안 보이는 두 번째 계산기가 생긴다.

**줄 시작 앵커가 요점이다** — 산문 속 `… blocked by #N …` 까지 집으면 정상 `대기` 가
`막힘` 으로 내려가, 막으려던 것보다 나쁜 방향(더 많이 잡는 쪽)으로 틀린다.

블로커 **상태**를 이미 받은 목록 안에서만 보므로 **목록 `--limit 200` 밖의 블로커는 해제로
보인다**(fail-open — 막힌 건이 `대기` 로 남는다 = 이 기능이 없던 때와 같은 상태이지 거짓
`막힘` 이 아니다). 그 신호는 기존 warn `목록 절단` 이 낸다. 다른 레포 번호는 지원하지
않는다(`eligible-issues.sh` 도 같은 레포만 본다).

`블로커 needs-human` warn 의 묶음 단위가 **블로커**인 이유(하위마다 한 줄이 아니라): 사람이
답할 것은 하나인데 줄이 여럿이면 같은 질문이 N번 울린다. `배포대기` 블로커도 같은 규칙 —
둘 다 사람이 답해야 풀리는 게이트라, 그때까지 하위는 루프가 아무리 돌아도 안 풀린다.
`issue-runner`·`검증대기`·`verify-runner`·`마감대기`·`closeout`·`보류`·`대기`·`막힘`
블로커와 PR 블로커는 warn 이 아니다 — 루프가 처리 중이라 사람이 할 일이 없다(`보류` 는
단독 `hold:conflict` 포함 — 재개 스윕이 풀 것, #345/#346).

### §6 warn 의 정의 — "루프가 교정 가능한 위반" (#188 · #190)

warn 은 **루프가 교정 가능한** 불변식 위반만이다 — 루프가 집을 수 없는 후보는 warn 이
아니다. **조치 불가능한 warn 은 신호를 죽인다**(#188). 그래서 warn 은 보고만 하고 교정하지
않는다.

이 정의에서 갈라져 나온 판정들:
- 사람 세션 PR(head 가 `agent/issue-*` 가 아니거나 `full-cycle` 라벨, #246)은 warn 에서
  빼되 **존재는 남긴다** — `note N` 줄 아래 한 줄씩(#188). 관측에서 사라진 게 아니라 warn 이
  아닌 자리로 간 것이다.
- 정지 미러 불일치(#265)는 **해제 방향만** 본다 — 반대(이슈에 있고 PR 에 없음)는 부착 축이라
  이 루프에 교정 수단이 없어 warn 정의(#190)를 벗어난다.
- 맨몸 `needs-human` 만 남은 PR 은 정지 미러 불일치 대상 밖이다 — 기계가 만들 수 없는
  모양이라 사람이 손으로 세운 브레이크이고 교정 갈래도 떼지 않는다(교정 못 하는 후보는
  잡음, #190).
- 재개 횟수를 못 셌으면 횟수를 생략하고 warn `재개 횟수 미확인`(#346) — 3상태다. `0/1` 은
  "0회" 라는 **주장**이라 모르는 채로 못 찍는다.
- 질문 유무를 못 봤으면 "질문 없음" 으로 접지 않고 모른다고 말한다(#157) — 실패를 "질문
  없음" 으로 접으면 없는 결함을 사람에게 들이민다.

### §7 무소속 PR · 인계 전 창 · 워커 사망 의심 (#177 · #181 · #246 · #282)

`hold:*` 를 정지 라벨에 함께 보는 이유는 #244 — 기계 정지가 `needs-human` 을 떼고 사유
라벨만 남기게 됐으므로, `needs-human` 만 보면 홀드된 PR 이 통째로 이 warn 으로 쏟아진다.

**인계 전 창(플랜 §5)** — 디스패치 직후 워커가 PR 을 열고 아직 단계 라벨을 못 찍은 정상
구간이 매 틱 warn 으로 울리는 걸 막는다. #282 로 `flow:claimed` 라벨이 생긴 뒤 이 창 규칙은
`flow:claimed` 가 없는 라벨 0 PR(옛 워커·사람이 연 PR)에만 남았다. 연결 이슈를 **라벨이
아니라 버킷으로** 보는 이유: 라벨로 걸면 `agent:claimed` 을 단 채 배포대기·flow:*·harvesting
으로 간 이슈의 PR 이 warn 에서만 빠지고 issue-runner 줄엔 안 그려져 아무 표시도 없이
사라진다. 후보 집합 하나를 둘로 **분할**하므로 표시와 warn 은 항상 서로 배타다 — 두 조건을
따로 쓰면 드리프트한다.

**경과 `<N>분` 이 PR 나이가 아니라 `agent:claimed` labeled 이벤트 기준인 이유 (#177):** PR
나이로 재면 홀드 해제 뒤 **재디스패치**된 건에서 숫자가 통째로 부풀어(실측 #170/PR #172:
231분 — 실제 claim 은 7분 전, 워커는 15분 뒤 인계까지 끝냈다) 살아 있는 워커를 사망으로
신고하고, 반송을 돌수록 숫자가 단조 증가해 신호가 죽는다. 반대로 재claim 직후 죽은 워커는
PR 이 방금 열렸으면 작게 나와 안 울린다. 창 분할만은 종전대로 PR 나이로 한다 — 둘은 다른
질문이다: 창은 "PR 을 연 뒤 얼마나 됐나", 꼬리표는 "워커를 붙인 뒤 얼마나 됐나".

시각을 못 얻으면 숫자를 지어내지 않고 미상으로 바꾸되(0분으로 접으면 진짜 사망이 숨는다)
**warn 자체는 유지**한다. 미상 안에서도 두 문구를 가르는 이유는 #181 — "안 봤다"(조회 상한)
와 "보고 실패했다"(확인 필요)는 다른 사실이라 사람이 다르게 반응해야 한다(전자는 상한을
늘릴 문제).

**`워커 사망 의심` 이 별도 줄인 이유 (#282):** 무소속 warn 이 라벨 0 PR 로 좁혀져 그 꼬리표가
`flow:claimed` PR 엔 닿지 않으므로, 경과 기준의 별도 줄이 필요했다 — "라벨이 붙어도 경과가
길면 뜬다". 후보는 PR 나이로 고르고(창 안 PR 은 타임라인을 안 본다 — 틱 비용) 판정은 claim
경과로 한다: 재디스패치 건은 PR 이 오래됐어도 claim 이 최근이라 **뜨지 않는다**(PR 나이로
울리면 #177 이 걷어낸 오탐이 이 줄로 되살아난다).

`CLAIM_TIME_MAX` 가 **고유 이슈 수**를 세는 이유도 #181 — 한 이슈에 무소속 PR 이 여럿이어도
상한은 한 번만 깎인다.

### §8 미러 불일치와 정지 미러 불일치 (#265 · #281 · #282 · #331)

이슈 `agent-ready` 와 PR `flow:agent-ready` 의 이름이 갈리는 이유는 `transition.sh` §2 와
같다 — 이슈의 `agent-ready` 는 자격 라벨이라 PR 의 단계 이름으로 못 쓴다. `flow:ci`·
`flow:codex` 는 PR 에만 있는 워커 내부 단계라 이슈 미러가 없다(= 미러 불일치 판정에
참여하지 않는다).

워커 칸 두 쌍이 **PR 이 그 라벨을 달고 있고 이슈가 정지 중이 아닐 때만** 대조에 드는 근거는
코드의 `mirror_scoped` 주석이다 — 라벨 0 PR 은 인계 전 창의 소유이고, 정지 중엔 홀드 전이가
그 둘을 일부러 안 맞춘다. 이슈 대기 칸 + PR `flow:claimed` 는 **재claim 대기**(홀드 해제·죽은
워커 claim 회수 뒤 — 다음 claim 이 수렴, #281) 라 warn 이 아니라 대기 줄에 붙는다.

**정지 미러 불일치(#265)** 는 사람이 이슈에서만 홀드를 푼 잔재이고, 그 PR 은 네 게이트
(#242·#262)에서 확정적으로 빠진 채 이슈도 깨끗해 어느 칸에도 안 뜬다. 짝을 head
`agent/issue-N` ∩ `closingIssuesReferences` 로만 잡는 이유는 BUILD_JQ 주석에 있다(사람 세션
PR·`Refs` 전용 PR 은 대상 밖). 그 PR 이 닫는 이슈가 **전부** 깨끗할 때만 내는 이유: 묶음
디스패치(`Closes #A`·`Closes #B`)는 정지가 한쪽에만 붙을 수 있다.

그 전건 판정이 **열린 이슈 ∪ 닫힌 이슈**를 보는 이유는 #331 — 교정 갈래는 닫는 이슈를
번호로 실제 조회해 CLOSED 도 판정하므로, 열린 목록만 보면 두 술어가 갈린다(추가 gh 호출은
여전히 0 — 닫힌 이슈 목록은 에픽 절이 이미 받아 온다).

단계 미러와 달리 짝이 되는 열린 PR 을 **전부** 대조하는 이유: 교정 갈래(`resume-sweep.sh` 의
정지 미러 정리)가 전부를 고친다. 그 교정 갈래엔 여기 **없는** 관문이 하나 더 있다(라벨 이벤트
이력으로 "사람이 뗐다" 를 증명) — 경보는 부재만으로 참인 사실을 말하고 교정은 증명을
요구한다. 이 비대칭의 근거는 `resume-sweep.sh` 의 ④ 머리 주석.

### §9 목록 절단 warn 에서 닫힌 이슈를 뺀 이유 (#292 / #190)

`닫힌 이슈`(최근 200건) 목록의 유일한 소비자였던 에픽 leaf 카운트가 검색 스코프 조회로 옮겨
갔고, 남은 쓰임(색인 지연 보완·조용한 실패 교차확인)은 최근 것만 있으면 되는 성질이라 상한
도달이 정상이다. bodat 은 매 틱 그 상한에 닿아(창 약 6일) 교정 불가능한 상시 소음을 냈다 —
#190 의 warn 정의와 어긋나고 진짜 절단 신호를 가린다.

### §10 에픽 절과 닫힌 leaf 의 창 (#260 · #292 · #441)

**leaf 판정 규칙의 스타일**(줄 앵커·대소문자 무시·첫 매치)은 `eligible-issues.sh`/
`blockers_of` 의 `Blocked by #N` 파싱과 같다 — 그 파일은 #247 워커 소유라 여기서는 고치지
않고 같은 스타일만 jq 로 옮겼다. 산문 속 `… epic #N …`(줄 시작이 아님)은 leaf 로 잡히지
않는다(과잉 포획 방지 — Blockers 픽스처의 #15/#21/#22 반증과 같은 이유).

**닫힌 leaf 의 P 를 분포에서 빼는 이유:** 과거라 못 고친다(§6 의 warn 정의와 같은 이유).

**조회 실패면 비율도 `leaf 없음` 도 찍지 않는 이유(#292):** 둘 다 "그렇다고 확인했다" 는
주장인데 실패한 조회는 그 주장을 못 한다. 이때 `에픽 leaf 전부 종료` warn 은 **그대로 뜬다**
— 그 판정은 "열린 leaf 0 + 닫힌 leaf ≥1" 이고 조회 실패는 닫힌 leaf 를 **적게만** 셀 수 있어
거짓 양성이 아니라 거짓 음성 방향이다(뜨면 참이다). 같은 에픽에 `종료 미상` 과 `전부 종료`
가 함께 보이는 건 모순이 아니라 "개수는 못 셌지만 열린 leaf 는 없다" 는 두 사실이다.

**`에픽 닫힌 leaf 조회 실패` warn 의 존재 이유:** 빈 결과(닫힌 leaf 가 정말 0건)와 실패를
가르는 것. 실패를 0건으로 접으면 `0/N` 이 되어 이 이슈가 고치려던 증상이 새 경로에서 그대로
재현된다. **탐지 범위(실측한 한계):** ⑴ 종료코드 실패와 ⑶ 배열 아닌 응답은 무조건 잡는다.
⑵ 조용한 빈 결과(빈 출력 + exit 0)는 **core API 창(최근 닫힌 200건)에 `Epic #N` 줄이 하나라도
있을 때만** 잡는다 — 판정 입력이 그 목록이기 때문이다. 그 창에 Epic 줄이 하나도 없는 레포에서
검색이 조용히 접히면 `0/N` 으로 남는다. 이 사각은 PR #320 본문 `## 사전 리뷰` 에 재현과 함께
적어 뒀다(닫는 방향: 0행일 때만 Epic 필터 없는 대조 검색 1회를 더 쳐서 "검색 자체가 접혔는가"
를 Epic 줄 존재와 무관하게 가른다).

**닫힌 leaf 의 창을 검색 스코프로 옮긴 이유(#292):** 종전(#260)엔 "가장 최근 닫힌 200건" 에서
골랐는데 bodat 실측으로 그 창이 약 6일이라(2026-09-11: 200건 = 09-05 ~ 09-11) 6일보다 오래 산
에픽은 leaf 가 창 밖에서 닫혀 `종료/전체` 가 조용히 작아지고, leaf 가 전부 창 밖이면 `leaf
없음(Epic 줄 미부착)` 이라는 사실과 다른 줄이 나오면서 `에픽 leaf 전부 종료` warn 이 **안
떴다**(그 warn 이 존재하는 이유가 정확히 오래 산 에픽을 잡는 것이다). 실측 대조(2026-09-12,
ggqgga/BodaT): 최근 200건에 든 `Epic #N` 닫힌 leaf 6건 → 새 조회 15건.

그 결과를 종전의 `--state closed --limit 200`(core API) 목록과 **합집합**으로 쓰는 이유:
후자는 ⓐ 검색 색인 지연(방금 닫힌 leaf) 보완 ⓑ 조용한 실패 교차확인의 판정 입력이다.
합집합이라 새 조회는 leaf 를 더할 뿐 빼지 못한다(최악이어도 종전 수치로 degrade). leaf
재확인은 종전 그대로 `epic_of` 하나가 한다 — 검색 `in:body` 는 산문 매치도 주므로(bodat 실측
118행 중 실제 `Epic #N` 줄은 15행) 검색은 후보를 좁히고 판정은 안 한다.

**`EPIC_CLOSED_LIMIT` 만 `0` 을 금지하는 이유:** `0` 은 "안 본다" 가 아니라 조회가 0행을
돌려주는 값이라, 이 이슈가 고치려던 `닫힌 leaf 0건` 을 환경변수로 되살린다.

**`에픽 leaf 전부 종료` 꼬리가 셋인 이유(#441):** 스윕 마커(`<!-- epic-sweep -->` 코멘트)가
없으면 스윕이 닫을 대상이고, 있으면 스윕이 한 번 닫았다가 되돌려진 에픽(사람 재오픈 또는
코멘트만 남은 실패 잔여)이라 스윕 게이트가 다시 닫지 않는다 → 사람 몫. 못 봤으면 어느 쪽이라고
단정하지 않는다.

**파생 병기(`(Epic #N)`/`(에픽 없음)`)에 게이트를 둔 이유:** 이 게이트가 없으면 에픽을 안 쓰는
레포까지 `파생` 줄 서식이 바뀌어 이 이슈의 무회귀 기준(`Epic #N` 이 하나도 없는 픽스처는 에픽
절 추가 외엔 출력이 한 글자도 안 바뀐다)을 깬다. leaf 0 인 에픽만 있고 `Epic #N` 을 단 이슈가
하나도 없는 레포에서까지 무관한 파생에 `(에픽 없음)` 을 붙이면 "에픽 밖으로 샌다" 는 관측
자체가 성립하지 않는 레포까지 서식이 바뀐다.

### §11 gh 호출 예산과 검색 금지의 예외 (#21 · #127 · #157 · #177 · #236 · #292 · #441)

블로커 판정(#248)·에픽 leaf 판정(#260)이 예산을 **한 호출도 늘리지 않는** 방법: 이슈 목록
`--json` 에 `body` 필드를 더하고(같은 한 번의 호출) 블로커 상태·에픽 소속은 이미 받은 열린/닫힌
이슈 목록의 본문·멤버십으로만 본다. 블로커·leaf 마다 `gh issue view` 를 치면 N+1 이라 이
기능의 요점이 깨진다.

예외 넷이 성립하는 이유:
- ①(#157) 라벨만으론 질문 유무를 알 수 없고, 대상은 "지금 사람을 기다리는 건" 이라 목록
  전체가 아니라 한 줌이다.
- ②(#177·#282) `agent:claimed` 부착 시각은 목록 API 에 없다. 대상은 인계 창을 넘긴 PR 이라
  정상적으로는 한 줌이고(창 안 PR 은 조회하지 않는다), 상한은 `CLAIM_TIME_MAX`.
- ③(#292) 에픽 닫힌 leaf — 아래 검색 예외.
- ④(#441) 대상은 "스윕이 곧 닫을(또는 닫았다 되돌려진) 에픽" 이라 한 줌이고, 넘는 후보는
  `미확인` 으로 남긴다(거짓 `스윕 대상` 을 만들지 않는다).

**`gh search` / `gh issue list --search` 가 원칙 금지인 이유:** 인덱스 지연 + 부정 라벨 오파싱
(#21, `eligible-issues.sh` 주석 참조). 예외는 에픽 닫힌 leaf 하나뿐이고(#292), **다음에 같은
예외를 요청받으면 이 셋을 다 만족하는지 봐라:**
1. 대안이 없다 — "최근 N건" 창으로는 오래 산 에픽의 leaf 를 원리적으로 못 본다.
2. 인덱스 지연이 무해하다 — 결과를 core API 목록과 **합집합**으로 쓰므로 방금 닫힌 leaf 는
   후자가 덮고, 검색은 더할 뿐 빼지 못한다. `epic-sweep.sh` 와 달리 여기서는 이 수치로 이슈를
   **닫지 않는다**(보고만 한다 — 오판의 대가가 다르다).
3. 라벨 오파싱이 없다 — 쿼리에 `label:`/`is:` 를 안 쓴다. 상태는 `--state closed` **플래그**로
   준다(#236: `gh` 가 쿼리 문자열의 `is:` 를 오파싱한 실측 전례).

**쿼터 실측(2026-09-12):** `gh issue list --search` 는 GraphQL 경로라 REST **search** 버킷
(30/분)을 한 점도 쓰지 않는다(REST search 프로브 사이에 이 조회를 3회 끼워도
`X-Ratelimit-Used` 는 프로브분만 올랐다). `gh api search/issues`(에픽마다 1회)를 골랐다면 열린
에픽 9건 × 세 루프 = 분당 27회로 그 버킷이 바닥났을 것이다.

**실패를 stdout 에도 남기는 이유:** 세 루프는 ④ Report 에 **stdout 만** 붙인다. stderr 로만
말하면 사유가 사라지고, 환경 실패의 exit 1 이 "레포 하나 조회 실패"(부분 실패)와 구분되지
않는다. `HANDOFF_GRACE_MIN` 등 형식 오류가 환경 실패로 죽는 것도 같은 규율 — jq 에 그대로
넘기면 레포별 "집계 실패" 로 위장된다. `HANDOFF_GRACE_MIN=0` 을 허용하는 이유: 창이 없으면
warn 이 **늘어나지** `--since 0h` 처럼 거짓 "깨끗함" 이 되지 않는다.

---

## finish-classify.sh

절 색인: §1 존재 이유 · §2 `no_verdict` 와 "초록" 의 정의 · §3 스테일 클록의 네 입력 ·
§4 진행 증거 게이트 · §5 증명 실패의 방향 · §6 head 조회 3값 보존 · §7 브랜치 이슈 추정 ·
§8 `FC_COMMENTS_FILE` 이 파일 경로인 이유 · §9 실무에서는 타임박스가 연다

### §1 존재 이유 (#88)

SKILL ② Maintain 규칙4 가 이 결과로 4a/4b/4c 를 분기한다 — SKILL prose 를 얇게 유지하고
결정적으로 테스트 가능하게 하려고 판별을 스크립트로 뽑았다.

**판별 근거:** 살아있는 워커는 `검증자 리뷰:` 코멘트 직후 수초 내 최종 판정을 찍는다. 최신
검증자가 CLEAN 인데 `STALE_FINISH_MIN` 넘게 최종 판정이 없으면 워커 사망 확실. 진행 중 fix
루프는 최신 검증자 코멘트가 recent 이거나 non-CLEAN 이라 자동 제외된다.

최신 `머지 판정:`/`검증자 리뷰:` 판정에 코멘트 배열의 **마지막 매칭**을 쓰는 이유는 재리뷰·
재판정 대비다. 한/영 병행 워커라 영문 접두(Merge verdict/Verifier review)도 본다.

### §2 `no_verdict` 계급 (#396) 과 "초록" 의 정의 (#421)

워커가 `머지 판정: 🔄` 를 찍기 **전**에 죽으면(handoff 이전 사망) PR 은 CI 초록인데 판정
코멘트가 0건이다. 그 칸은 세 레인 어디에도 안 들어갔다 — issue-runner ② Maintain 은 "CI
green·리뷰 없음" 을 사람 리뷰 대기로 무접촉, closeout ①-b 는 🔄/✅ 를 전제, 이 파일은 판정이
없으면 `active` 였다(무한 무접촉). 그래서 **판정 부재 + CI 초록 + 증거 없음 + 시간버퍼 초과**를
별도 계급으로 냈다.

**"초록" 이 실패 0 이 아니라 실패 0 + 미완료 0 인 이유(#421 [P2-2]):** 이 계급의 주장은 "CI 는
끝났는데 판정만 없다" 이므로, 아직 도는 체크(check-run `status != COMPLETED` · local-ci 커밋
상태 `pending`)가 하나라도 있으면 주장할 수 없다 — 그 창에 커밋·claim 만 낡으면 **아직 초록도
아닌 PR** 이 재디스패치된다(CI 큐 대기 중인 PR 이 정확히 그 모양이다, #127·#200). 롤업 조회
실패도 같은 처분(`ci_lookup=unknown` → active) — 이 파일의 "증명 못 하면 안 연다" 규율.

판정 코멘트 0건을 **접두 매칭 개수 0** 으로 확인하는 이유: 코멘트 조회 실패는
`comments_lookup`, 못 읽는 판정 본문은 개수 ≥ 1 이라 둘 다 이 계급이 아니다.

### §3 스테일 클록의 입력 (#110 · #308)

**커밋 시각이 클록에 합류한 이유(#110, 실증 BodaT PR #2237):** 워커 활동 = 코멘트 **또는
커밋**. bounce 후 attempt N+1 워커는 같은 브랜치에서 이어가며 커밋은 하되 종료 직전에만 판정
코멘트를 찍는다 — 코멘트 시각만 보면 낡은 🔄 만 남아 살아있는 워커를 사망(`stale_reverify`)으로
오판한다.

**반송 마커 시각이 합류한 이유(#308):** 반송 전이는 `agent:claimed` 를 떼므로 반송 직후
디스패처가 다시 붙이기 전 창에서는 진행 증거 세 축이 전부 "old/none" 이다 — 그 창의
CONFLICTING PR 이 `stale_reverify` 로 떨어져 **사인이 틀린 멱등 마커**(`완결 유실(검증 전 사망)`)
를 원장에 남겼다. 마커 판별은 `bounce-state.sh` 한 자리를 되물어 얻는다(로직 두 벌 금지) —
코드의 `bounce_epoch_after` 주석 참조.

### §4 진행 증거 게이트 (#206)

🔄 계열 두 갈래(`stale_inline`·`stale_reverify`)는 "워커가 죽었다" 는 주장이다. 그 주장을 내기
전에 `progress-evidence.sh` 에 워커가 살아 있다는 증거가 있는지 묻고, 있으면 `active` 로
떨어뜨린다.

증거 ②(CI 큐 티켓)가 특히 중요하다: 박스 전역 직렬 CI 큐(#127) 대기는 워커가 통제할 수 없는
시간이라 커밋이 한 시간 넘게 멈춰 있어도 워커는 살아 있다(#200 실측 72분·64분).

술어는 `progress-evidence.sh` **한 자리**에 있다 — `timebox-check.sh`(#200)가 부르는 그 자리다.
두 벌로 복제하면 사람 눈에 안 보이는 두 번째 계산기가 다른 수를 센다.

**증거 ③(claim 부착 시각)이 덮는 창(#206 attempt 2 codex BLOCKER):** ①②는 워커가 이미 뭔가 남긴
뒤에만 존재하는 증거라, 반송 직후 교체 워커가 디스패치됐지만 아직 아무것도 push 하지 않은
구간에서는 둘 다 없다 — 그때 이 파일이 보는 값(판정 시각·head 시각)은 전부 **이전 attempt** 의
것이고 `STALE_FINISH_MIN` 을 넘겨 `stale_reverify` 가 난다. 그러면 `closeout-redispatch` 가
**지금 일하고 있는 워커의 `agent:claimed` 를 떼어낸다.** 그 창을 덮는 유일한 신호가 "이번 회차가
언제 시작됐나" = `agent:claimed` 부착 시각이고, 조회는 `claim-at.sh` **한 자리**다(존재가 아니라
시각을 쓰는 이유는 그 파일 주석 참조).

### §5 증명 실패의 방향 — fail-closed 와 `active` 가 같은 원리인 이유 (PR#139)

판정 실패(헬퍼가 `unknown` 이거나 아예 못 돔)는 "증거 없음" 이 **아니다** — 그 방향으로 접으면
조회 실패 한 번에 살아 있는 워커의 브랜치를 채간다(되돌릴 수 없는 손해). 그래서 여기선 증명
실패를 `active`(무접촉) 로 받는다. ✅ 갈래의 fail-closed 와 방향이 반대로 보이지만 **같은
원리**다: 되돌릴 수 없는 쪽(머지·재디스패치)을 증명 없이 열지 않는다.

`✅` 신선도를 증명 못 하면 `active` 인 것도 같은 규율이다(#171) — 반송 뒤 재디스패치된 새 커밋이
아직 검증 안 된 경우.

### §6 head 조회의 3값 보존 (#206 회차2)

그 규율은 **판정 입력을 얻는 자리**에서 시작한다. head 조회는 세 결말을 갖는다 — `ok`(값을
얻음) · `unknown`(조회 실패) · `none`(조회는 됐는데 커밋 증거 없음). 셋을 `head_sha=none` 하나에
실으면 하류가 실패를 부재로 읽어 살아 있는 워커를 재디스패치한다. 그래서 조회의 **종료코드를
보존**해 `head_lookup` 플래그로 기억하고, 진행 증거 헬퍼에도
같은 3값 어휘(`--commit-at unknown`)로 넘긴다.

`FC_HEAD_AT` 주입 경로가 `unknown` 이 아닌 이유: 호출자가 값을 준 것이므로 조회 실패 축은
실호출 경로에서만 난다. 테스트도 **실호출 자리를 스텁으로 물려** 문다 — 주입만 무는 테스트는
그 자리의 회귀에 눈먼다.

### §7 브랜치 이슈 추정 (#495 · #206 회차3)

`closingIssuesReferences[0]` 은 닫는 이슈가 둘 이상일 때 남의 이슈를 가리킨다. 그래서 head 의
`agent/issue-N` 을 1순위로 보고(`lib/loop.jq` 의 `linked_issue`), `[0]` 은 쓰지 않는다.

### §8 `FC_COMMENTS_FILE` 이 파일 경로인 이유 (#171 반송 4회차 [P2])

코멘트 전량을 환경변수 하나로 넘기면 exec 한계(리눅스 MAX_ARG_STRLEN 128KB)를 넘는 순간 이
스크립트가 **시작조차 못 하고** 호출자의 판정이 비어 그 PR 이 매 스윕에서 조용히 빠진다. 읽기
실패가 실조회로 새지 않고 빈 코멘트(= active, fail-closed)로 떨어지는 것도 같은 규율.

기본 조회가 `pr-comments.sh` **페이지네이션 전량**인 이유도 #171 — `gh pr view --json comments`
는 첫 100건 상한이 있다.

### §9 실무에서 이 계급을 여는 것은 시간버퍼가 아니라 타임박스다

살아 있는 회차는 `agent:claimed` 가 붙어 있어 진행 증거 ③(claim 이 `ISSUE_TIMEBOX_HOURS` 안)이
참이므로, claim 이 그 상한을 넘길 때까지는 `active` 다. **버퍼(`STALE_FINISH_MIN`)만 보고 "왜 안
걸리나" 를 고치려 들지 마라** — 그 게이트가 살아 있는 워커의 브랜치를 지킨다.

---

## transition.sh

절 색인: §1 전이를 한 자리로 묶은 이유 · §2 PR 미러 이름이 갈리는 이유와 보장 범위 ·
§3 `verifying` 점유 라벨 · §4 `agent-ready` 자격 라벨 · §5 `needs-human` 과 `hold:*` 를
가른 이유 · §6 closeout-dup 에서 PR close 가 마지막인 이유 · §7 사람 대기 세 전이의 코멘트
선행 · §8 라벨 부재 재시도

### §1 전이를 한 자리로 묶은 이유

루프 전이 = 라벨 이동. 세 루프(issue-runner·verify-runner·closeout)가 산문으로 흩어서 하던
`gh issue edit`(PR + 연결 이슈 미러)를 **전이 하나 = 호출 하나**로 묶는다. 흩어져 있으면 한쪽만
고쳐져 PR 과 이슈의 라벨이 갈리고, 다음 틱이 어긋난 쪽을 진실로 읽는다.

상태 단위(누가 어느 상태를 소유하고 반쯤 실패한 전이를 누가 회수하나)는
`references/state-machine.md`(#393) — 머리 주석의 전이 표와 1:1.

### §2 PR 미러 이름이 갈리는 이유와 보장 범위 (#281 · #420 · #265)

PR 은 이슈 사다리를 **전 칸** 미러한다(#281) — 이슈 `agent-ready`(대기) ↔ PR
`flow:agent-ready`, 이슈 `agent:claimed`(issue-runner) ↔ PR `flow:claimed`, 그 뒤 칸은 같은
이름. **이름을 달리 한 이유:** 이슈의 `agent-ready` 는 사다리 내내 남는 **자격** 라벨이라 같은
이름을 PR 의 **단계** 로 쓰면 뜻이 갈린다. 반송 두 전이가 PR 에 `flow:agent-ready` 를 붙이고
(대기 칸), claim(`claim-issue.sh`)이 그것을 `flow:claimed` 로 바꾸며, 워커가 처음 여는 PR 도
`--label flow:claimed` 로 태어난다. 그래서 열린 agent PR 은 항상 어느 칸의 라벨을 하나 달고
있다(라벨 없는 열린 agent PR = 사고).

**보장 범위(#420):** 머리 주석 전이 표의 전이(반송·인계·정지는 `hold:<사유>` 가 칸이다) + claim +
워커의 첫 PR + 재개 스윕의 **자동 재개 경로**(H:ladder→S0 — `resume-sweep.sh` 가 이슈의
`hold:ladder` 를 풀며 PR 의 `hold:ladder` 를 뗄 때 이슈 칸에 맞는 미러를 되붙인다; 칸 라벨이 이미
있으면 겹치지 않는다) + 디스패처 Maintain 규칙0(두 미러가 붙은 PR 은 건너뛴다).
**보장 밖:** 사람이 이슈의 `hold:policy`·`hold:conflict` 를 직접 푼 뒤 정지 미러 정리(#265)가 PR
의 사본만 떼는 경로(미러를 되붙이지 않는다), 그리고 라벨 손편집.

`⊘wk` 를 `handoff-verify` 에도 두는 건 사람이 claim 을 안 거치고 직접 인계하는 경우의 방어이고,
`verify-pick`·`closeout-pick`·`closeout-dup` 의 것은 방어적 제거다(정상 흐름에선 이미 없다).
`flow:ci`(로컬 CI 재실행 중)는 `flow:claimed` 안의 워커 내부 하위 상태라 둘이 같이 붙는다(정상).

### §3 `verifying` 점유 라벨 (#275)

`verifying` 은 verify-runner 의 **점유** 라벨 — closeout 의 `harvesting` 과 같은 자리다(PR + 연결
이슈 양쪽). `verify-pick` 이 집는 순간 `flow:verify`(검증대기)를 이것으로 바꾸고, 집은 뒤의
**모든 출구**(verify-pass·verify-redispatch·verify-held·closeout-pick·closeout-blocked·
closeout-redispatch·closeout-dup)가 뗀다 — 하나라도 빠지면 단계 라벨이 겹쳐 남아 다음 틱이
고아로 재집는다. `verify-unpick` 은 pick 의 정확한 역: flake_retry(E2E 인프라 흔들림 — 판정
아님)로 끝나면 점유를 풀고 검증대기로 되돌린다.

그래서 "`verifying` = 지금 이 순간 검증이 돌고 있다" 가 항상 참이고, 틱 시작에 남아 있는
`verifying` 은 이전 틱이 죽은 것이다(`verify-eligible.sh` 가 그걸 먼저 낸다, `orphan:true`).

### §4 `agent-ready` 자격 라벨 (#117)

`agent-ready` 는 사다리 전체에서 유지되는 **자격** 라벨이다. 근거는 `release-labels.sh` 의 #117:
OPEN 이슈의 `agent-ready` 를 떼면 디스패치 자격만 사라져 조용히 좌초한다. 예외는
`closeout-dup` 뿐 — 이슈를 **닫으므로** `release-labels.sh` 가 회수한다.

PR 의 `flow:agent-ready` 는 이것과 다르다 — 자격이 아니라 **단계**(대기 칸) 라벨이라 claim 이
떼고 `flow:claimed` 로 바꾼다(#281). 이름에 `agent-ready` 가 들어 있어도 이 불변식의 대상이
아니다.

### §5 `needs-human` 과 `hold:*` 를 가른 이유 (#244 · #147 · #155)

겹쳐 붙이던 옛 표에서는 `needs-human` 이 "사람 호출" 이 아니라 "루프 손대지 마" 로 읽혀, 사람
대시보드의 needs-human 칸이 **손댈 게 없는 것**(창이 지나면 루프가 스스로 재개하는
`hold:ladder`)으로 찼다. 그래서 #244(플랜 3단계)로 기계 정지 세 전이(verify-held·
closeout-blocked·runner-held)는 `hold:<사유>` 만 붙이고, 루프가 `needs-human` 을 붙이는 곳은
`policy-kept` 하나뿐이다 — `hold:policy` 재심(#155)이 "사람 몫 유지" 로 끝났을 때.

기계 정지 세 전이가 **PR 에도** 사유 라벨을 붙이는 이유: 연결 이슈 없는 PR(`issue=-`)은 정식 호출
형태인데, 이슈에만 붙이면 정지 신호가 아무 데도 안 남고 exit 0 `ok` 로 끝나 조용히 사라진다.

**정지 사유가 라벨인 이유(#147):** 사유가 없으면 왜 멈췄는지 목록에서 안 보여 쓰레기통이 된다.
그래서 `--reason` 이 필수고, `dup`·`hardware` 는 사유가 될 수 없다 — 중복은 `closeout-dup` 이
닫고, 실장비는 검증 사다리를 오른다.

반송 두 전이가 `needs-human`·`hold:*` 를 떼는 이유: 반송 = 사람 대기 해제. 재개 스윕(#147 §4)이
이 전이로 멈춘 건을 다시 태운다.

### §6 closeout-dup 에서 PR close 가 마지막인 이유

먼저 닫으면 뒤(③④)가 실패했을 때 PR 이 CLOSED 라 closeout 의 다음 틱이 **다시 집지
못한다**(복구 불능). 마지막에 두면 중간 실패 시 PR 은 `dup` 라벨을 단 채 열려 남고, 다음 틱이
같은 전이를 다시 걸어 완주한다. 그래서 ①~④ 는 전부 멱등이어야 한다 — 라벨 편집은 원래
멱등이고, 코멘트는 중복을 감수한다(근거가 두 번 남는 쪽이 안 남는 쪽보다 낫다).

`needs-human` 을 쓰지 않는 이유: 이슈가 요구한 수정이 이미 main 에 있으면 **루프가 결정할 수
있는 건 루프가 끝낸다.**

### §7 사람 대기 세 전이의 코멘트 선행 (#157)

코멘트가 뒤였을 땐 코멘트 API 의 일시 실패가 "라벨은 붙었는데 질문이 없는 홀드" 를 남겼고(재시도
주체 없음), 앞으로 옮기면 그 실패가 라벨 편집 전에 exit 2 로 끝나 상태가 전이 이전 그대로 남는다
— 호출부가 다음 틱에 같은 전이를 다시 걸면 그게 곧 재시도다. `--note` 가 없는 전이는 이 단계를
아예 거치지 않으므로 종전 경로 그대로다.

### §8 라벨 부재 재시도 (실측 2026-09-09)

레포에 **없는** 라벨은 `--remove-label` 도 편집 전체를 실패시킨다 — gh 메시지는
`'hold:conflict' not found` 형식이라 "label" 단어가 없다(따옴표 형식도 잡아야 한다). 그래서
`setup-labels.sh` 를 **프로세스당 1회** 돌리고 같은 edit 를 **1회만** 재시도한다(무한루프 금지).
그 이상은 다음 틱이 재시도한다 — `references/loop-conventions.md` 「폴백 결말」 표와 같은 규율.

---

## bounce-state.sh

절 색인: §1 존재 이유와 소비자 넷 · §2 `held` 값이 생긴 경위 · §3 스윕 재부착 폐지 ·
§4 `--marker-index` 가 같은 jq 한 판에서 나오는 이유 · §5 축을 섞지 않는다 ·
§6 `BOUNCE_COMMENTS_FILE` 읽기 실패가 exit 1 인 이유

### §1 존재 이유와 소비자 넷 (#196 · #271)

소비자가 넷이고 실행 주체가 다르다:
1. `closeout-eligible.sh` — ✅ 정상 후보 필터의 마지막 관문(셸에서 호출)
2. `skills/closeout/SKILL.md` ①-b 의 **CONFLICTING 입양** 경로 — 프로즈가 이 스크립트를 직접
   부른다. 입양(rebase)은 `finish-classify.sh` 의 다섯 갈래 중 어디에도 없는 조치라 반송 판정만
   따로 빌릴 자리가 필요했다. (#206 이후: 그 경로에서 `bounced` 는 **무접촉으로 끝나지 않고**
   finish-classify 로 넘어가 `stale_reverify` 만 재디스패치로 갈라진다. 이 스크립트의 판정
   자체는 그대로다 — 입양을 여는 것은 여전히 `ok` 뿐이다.)
3. `skills/closeout/SKILL.md` ① Reconcile 의 **`resume` 재개 지점**(#271) — 프로즈가 마커표를
   보기 **전에** 부른다. 여기서 `bounced` 는 능동 전이 재시도다.
4. `closeout-step1-marker.sh`(#271) — `--marker-index` 로 선후 비교에만 쓴다.

**호출자별 계약을 나눈 이유(#271):** 소비자 3이 생기며 옛 한 줄 계약 "ok 일 때만 정상 진행 ·
나머지는 전부 무접촉" 이 거짓이 됐다. 거기선 `bounced` 가 **능동 전이 재시도**의 신호다.

**마커 집합과 선후 판정을 절대 두 벌로 두지 않는 이유(#171):** 예전에 채널마다 가드를 베껴
한쪽(verify-runner 채널)이 빠진 채 fail-open 이었다. 그 한 자리가 여기다.

### §2 `held` 값이 생긴 경위 (#218 attempt 2·3·4)

- **attempt 2 — codex BLOCKER:** 반송을 무조건 조기 종료하면 반송 뒤 워커가 명시적으로 올린 보류
  신호가 묻힌다. 그래서 `held` 를 갈랐다.
- **attempt 3 — codex BLOCKER:** ✅ 의 *존재*만 보고 그 뒤에 더 늦은 ⚠ 을 못 봐 `반송 → ✅ → ⚠`
  이 `ok` 로 샜다. 늦은 쪽이 이기도록 인덱스를 직접 비교해 고쳤다.
- **attempt 4 — 마감 검증 BLOCKER:** 후보 집합에 `🔄` 가 없어 `held` 가 **한 번 걸리면 안
  풀렸다**. 해제 경로는 코드의 "해제 경로" 절이 소유한다.
  교훈(레포 공통): **새 종결 상태를 만들 때는 진입 경로만 보지 말고 해제 경로까지 같이 설계하라.**

### §3 스윕 재부착 폐지 (#218 두 번째 회차 · 사람 결정 (c))

이 값 자체(계산)는 바뀌지 않았지만, closeout 스윕(gate 1)은 더 이상 `held` 를 "needs-human 승격"
신호로 쓰지 않는다 — `bounced` 와 똑같이 무접촉으로 받는다. 그래서 `held` 를 특별히 갈라
needs-human 으로 승격하던 호출자 계약도 함께 폐지됐다. 자세한 경위는 코드의 "스윕 재부착 폐지"
절.

(`finish-classify` 자신의 `held` 행 — 반송 마커가 **없는** 순수 ⚠ — 은 이 파일 밖의 별개 판정이라
영향받지 않는다.)

### §4 `--marker-index` 가 같은 jq 한 판에서 나오는 이유 (#171)

소비자 3이 "이 코멘트가 반송보다 뒤인가" 를 물어야 하는데, 그 질문에 답하려고 마커 매칭을 한 벌
더 쓰면 #171 이 막은 사고(채널마다 가드를 베껴 한쪽이 빠진 채 fail-open)가 그대로 돌아온다. 두
모드는 **같은 jq 한 판**에서 나온다 — 값이 갈릴 수 없다.

### §5 축을 섞지 않는다 — finish-classify 에 출력을 더하지 않은 이유 (PR#168)

`finish-classify.sh` 에 출력 하나를 더 얹는 길도 있었지만 택하지 않았다 — 거기 다섯 출력은 "완결
유실 상태" 라는 한 축의 값이고, 반송 여부는 **직교하는 다른 축**이다. 한 stdout 에 두 축을 섞으면
소비자가 문자열로 축을 다시 갈라야 한다(PR#168 교훈: 사유가 여럿인데 공유 센티널 하나만 보면
조용히 오분류된다). 그래서 축마다 한 자리다.

**판정 실패(exit 1)를 통과로 바꾸지 않는 이유(PR#139):** 반송되지 않았음을 *증명하지 못한* 상태를
통과로 처리하면 그게 fail-open 이다 — 빈 결과와 실패를 구분하고, 실패는 가드 분기로 보내라.

### §6 `BOUNCE_COMMENTS_FILE` 읽기 실패가 exit 1 인 이유

읽기 실패·빈 파일이 **실조회로 새지 않는** 이유: 호출자가 "이 파일이 곧 판정 입력" 이라 계약한
이상 다른 출처로 조용히 갈아타면 무엇으로 판정했는지 알 수 없다. 코멘트 0건은 `[]` 로 오며, 그건
정상 판정(`ok`)이라 빈 문자열과 구분한다.

파일 경로인 이유는 `finish-classify.sh` 의 `FC_COMMENTS_FILE` 과 같다 — 전량 코멘트가 exec 인자
한계 128KB 를 넘을 수 있다(finish-classify.sh §8).

---

## progress-evidence.sh

절 색인: §1 왜 스크립트로 뽑았나 · §2 3값 어휘를 입력 쪽에도 둔 이유 · §3 증거 ③ 이 덮는 창과
상한 · §4 queue.log 는 그 SHA 의 마지막 줄

### §1 왜 스크립트로 뽑았나 (#200 → #206)

이 술어의 소비자가 둘이 됐다:
1. `timebox-check.sh`(#200) — ① Reconcile 의 timebox 판정. 원래 주인.
2. `finish-classify.sh`(#206) — 완결 유실 갈래에서 "🔄 가 낡았지만 워커가 살아 있는가" 를 묻는다.
   커밋 신선도를 빼먹으면 **살아 있는 attempt N+1 워커를 죽인다**(이 레포의 반복 오탐).

두 벌로 복제하면 사람 눈에 안 보이는 두 번째 계산기가 다른 수를 센다 — 마커 집합을
`bounce-state.sh` 한 자리에 묶은 것과 같은 규율이다(#171·#196, PR#191 교훈). `bin/ci` 가
`^STALL_MIN=`·`^queue_alive()` 정의가 이 파일 밖에 생기면 실패시킨다.

### §2 3값 어휘를 입력 쪽에도 둔 이유 (#206 회차2 · PR#139)

호출자가 `unknown` 을 `none` 으로 접으면 조회 실패가 "증거 없음" 으로 둔갑해, 이 파일이 지키려는
출력 계약이 호출부에서 깨진다. 그래서 어휘를 입력 쪽에도 둔다 — **실패를 표현할 말이 없으면
호출자는 결국 `none` 을 쓴다**(회차1 이 그렇게 반송됐다).

`unknown` 을 `none` 과 섞지 않는 이유: 조회 실패로 살아있는 워커를 죽이는 것은 되돌릴 수 없는
손해다. 빈 결과와 실패를 구분해(PR#139) 실패는 호출자가 보수적으로 받는다.

### §3 증거 ③ 이 덮는 창과 상한 (#206 attempt 2·3)

①②는 워커가 **이미 뭔가 남긴 뒤에만** 존재하는 증거다. 반송 직후 교체 워커가 디스패치됐지만
**첫 푸시 전**인 창에는 둘 다 없고, 그때 호출자가 보는 커밋·판정 시각은 전부 *이전* attempt 의
것이다 — 그래서 ①②만으로는 살아있는 워커를 죽인다(#206 attempt 2 codex BLOCKER). ③이 그 창을
덮는다.

**상한이 `ISSUE_TIMEBOX_HOURS` 인 이유:** 그 시간을 넘긴 claim 은 `timebox-check.sh`(① Reconcile)
가 이미 회수 대상으로 보는 구간이라 여기서 살릴 이유가 없다 — 두 자리가 같은 상수를 읽어 같은
경계를 쓴다(정의는 `scripts/lib/constants.sh`, #427 로 한 자리로. 종전엔 두 파일이 각자 기본값을
들고 `bin/ci` 가 대조했다).

③이 **옵션 입력**인 이유: `--claimed-at` 을 안 주면 종전과 똑같이 ①②만으로 판정한다.
`timebox-check.sh` 는 자기 경과 검사로 이미 같은 경계를 재므로 넘기지 않는다. 부착 시각은
`claim-at.sh` 가 낸다(#206 attempt 3).

### §4 queue.log 는 그 SHA 의 마지막 줄 (#127 · 실측)

큐 대기는 **박스 전역 직렬 CI 큐(#127)** 대기라 워커가 통제할 수 없는 시간이다(실측 bodat #5020
72분 · #5024 64분, 둘 다 살아 있었다).

판정을 반드시 **그 SHA 의 마지막 줄**로 하는 이유: 같은 SHA 가 여러 줄인 것이 정상이다 —
`폐기 — 실행 시점 HEAD 가 …` · `중단(INT/TERM)` · `유령 티켓 회수(pid 사망)` 뒤의 재큐는 정상
동작이라, "어딘가에 대기열 줄이 있나" 로 재면 이미 끝난 티켓을 살아 있다고 읽는다.
