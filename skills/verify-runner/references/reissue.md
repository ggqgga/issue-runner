<!-- 이 파일은 **이슈 본문 전용** 템플릿이다 — 라벨을 여기 적지 마라(본문에 렌더된다).
     라벨은 `scripts/reissue-pr.sh` 가 원 이슈에서 상속해 발행 명령줄로 준다(`agent-ready` 포함).
     `spinoff` 는 **달지 않는다** — 재발행은 파생이 아니라 **재시도**다(#301). -->
재발행: PR #<PR> ← #<ISSUE>
<EPIC_LINE>

## 왜 다시 내는가

검증 재디스패치 상한(`VERIFY_ATTEMPTS_LIMIT`)에 닿아 PR #<PR> 을 닫고 이 이슈로 다시 낸다.
누적된 반송 문맥을 물려받은 워커보다 **검증자의 마지막 스펙을 본문으로 받은 새 워커**가 빠르다
(2026-09-11 실측: 상한에 닿은 네 PR 에 사람이 전부 한 회차를 더 줬고 그중 셋이 그 회차도
BLOCKER 로 끝났다 — BLOCKER 가 고쳐진 게 아니라 위치만 옮겨 다녔다).

## 이전 시도 — PR #<PR>

- 브랜치 `<BRANCH>` 는 **지우지 않았다**. 마지막 head: `<HEAD_SHA>`
- 참고는 하되 **그대로 이어받지 않는다** — 같은 문맥에 회차를 얹는 것이 이 재발행이 끊으려는 고리다.
- 마지막 반송 사유: <LAST_BOUNCE>

## 검증자가 마지막으로 남긴 것 (BLOCKER/WARN — 원문 그대로)

<VERIFIER_FINDINGS>

## 원 이슈 #<ISSUE> 본문 (수용 기준 포함 — 원문 그대로)

<ISSUE_BODY>
