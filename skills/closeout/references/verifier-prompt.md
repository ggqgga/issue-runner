PR #<PR> (<REPO>)의 변경이 <PLAN_REF> 를 실제로 구현했는지 검토하라.
**아래에 동봉한 diff 와 이슈 본문이 유일한 판정 근거(SSOT)다. gh·git 등 어떤
명령도 새로 실행하지 마라** — 네트워크 명령(gh·git fetch)뿐 아니라 로컬
`git grep`·`git log`·`git show` 등 워킹 체크아웃 조회도 금지한다. 이 세션의
로컬 main 은 PR 이 벌어진 base 보다 뒤처져 있을 수 있어(직전 머지가 아직
반영 안 됨) 이미 제거된 참조를 "살아있다"고 오판하게 만든다(실증: stale
체크아웃발 오탐 BLOCKER). 오직 아래 텍스트만으로 판정하라. (1) 계획/수용 기준
대비 미구현·일탈 (2) 범위 초과(요청 안 한 변경) (3) 과잉 설계. read-only(코드
변경 금지), 한국어, 발견마다 BLOCKER/WARN/NIT 분류. 부합하면 'CLEAN'.
<PLAN_REF> 가 비면(단일 이슈) 아래 이슈 본문의 수용 기준 + Test plan 을 대조
기준으로 쓰라. 단, Test plan 중 "배포 후 라이브 검증"·하드웨어/실장비 검증처럼
**머지 후에만 수행 가능하다고 표기된 항목**은 머지 게이트 대상이 아니다 —
미수행을 BLOCKER 로 분류하지 마라 (closeout 4단계가 배포 이슈의 "라이브/하드웨어
검증 항목"으로 이관해 배포 후 검증한다).

**diff 바깥 상태 다운그레이드 규칙 (중요).** dead-code 여부·dangling
reference·"이 심볼이 다른 곳에서 아직 참조되나" 같은 판정은 동봉된 diff 만으로는
확정할 수 없다(diff 는 변경분만 담고 나머지 코드베이스는 안 보인다). 이런
diff-바깥 사실에 근거한 지적은 **BLOCKER 로 올리지 말고 WARN 으로 내려** 사유에
`diff만으로 확인 불가 — 나머지 코드베이스 미확인` 을 명시하라. diff 안에서 자체
모순(추가한 호출의 정의가 없음 등)이 보이는 경우에만 BLOCKER 로 판정한다.

--- diff (`git diff <BASE>...HEAD`) ---
<DIFF>

--- 이슈 본문 ---
<ISSUE_BODY>
